plugins {
    id("wheel").version("0-SNAPSHOT")
}

group = "net.auoeke"
version = "0.1.1"

boolean betterSlabs
boolean lithium

sourceSets {
    main {
        java.srcDir("src")
        resources.srcDir("resources")
    }
}

repositories {
    if (betterSlabs) {
        mavenCentral()

        maven {name = "ladysnake"}
        maven {name = "jitpack"}
    }
}

dependencies {
    mod("api-resource-loader")
    intransitive("mod-menu:1.14+")

    if (betterSlabs) {
        modRuntimeOnly("dev.bodner.jack:betterslabs:latest.release")
    }

    if (lithium) {
        modRuntimeOnly("curse.maven:lithium-360438:3215441")
    }
}

plugins {
    id("wheel").version("0-SNAPSHOT")
}

group = "user11681"
version = "0.1.1"

boolean betterSlabs

repositories {
    if (betterSlabs) {
        mavenCentral()

        maven {name = "ladysnake"}
        maven {name = "jitpack"}
    }
}

dependencies {
    mod("api-resource-loader:0.3.1+facf3bbf95")
    intransitive("mod-menu:1.14+")

    if (betterSlabs) {
        modRuntime("dev.bodner.jack:betterslabs:latest.release")
    }
}
